package com.ruben.WeatherApp.Utils;

import org.json.simple.parser.JSONParser;

public class JsonUtils {
	
	public static void readJsonFromUrl(String url) {
		Object obj;
		obj = new JSONParser().parse(InternetUtils.readUrl(url, token));
	}
}
